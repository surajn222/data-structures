# Sol1:
# Take the first and second variable as min possible
# Traverse the array
# 1. If current element > first variable, then
# second variable = first variable and first variable = current element
# 2. If arr[i] is in between first and second then
# update second